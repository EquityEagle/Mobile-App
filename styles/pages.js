import { StyleSheet } from "react-native";
import { globalStyles } from "./global";

export const StyledView = StyleSheet.create({
  container: {
    // padding: 10,
    position: "relative",
    width: "100%",
    height: "100%",
    backgroundColor: "#000",
    // backgroundColor: globalStyles.colors.bg,
  },
});
