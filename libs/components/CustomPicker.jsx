import React, { useState } from "react";
import {
  View,
  Text,
  TouchableOpacity,
  Modal,
  FlatList,
  StyleSheet,
} from "react-native";
import Pairs from "./Pairs";

const CustomPicker = ({ modalVisible, setModalVisible, setSelectedPair }) => {
  return (
    <View style={styles.container}>
      <Modal
        animationType="slide"
        transparent={true}
        visible={modalVisible}
        onRequestClose={() => setModalVisible(false)}
      >
        <View style={styles.modalContainer}>
          <Pairs
            setSelectedPair={setSelectedPair}
            setModalVisible={setModalVisible}
          />
        </View>
      </Modal>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  modalContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "rgba(0, 0, 0, 0.5)",
  },
});

export default CustomPicker;

// Example usage:
// <CustomPicker options={['Option 1', 'Option 2', 'Option 3']} selectedValue={selectedValue} onSelect={handleSelect} />
